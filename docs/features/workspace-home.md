# 首页与工作区（Workspace & Home）

> `[反推]` 路由：`/`、`/app`、`/project/*`。源码：`src/features/home/`、`workspace/`、`landing/`、`settings/`。

## 用户故事

- 作为用户，我希望管理本地项目、从模板创建、打开文件夹或 zip。
- 作为用户，我希望在工作区内按**桌游创作**的几个维度工作：卡牌、板件、包装盒、说明书。
- 作为用户，我希望卡牌里再进蓝图、数据集、卡组，并自动保存。
- 作为新用户，我希望有 starter 模板和 onboarding 引导。

## 界面与交互

### 落地页（/）

`[反推]`

- 品牌 Hero、三步介绍、特性卡片
- **两个**离线包下载：Windows `TMD-offline.zip`、Mac（Apple 芯片）`TMD-offline-mac.zip`（有文件才可点）。步骤须分别写 bat / `.command`。详见 [offline-pack.md](./offline-pack.md)
- 「进入工作室」→ `/app`

### 工作室首页（/app）

`[反推]` HomePage 多 Tab（homeViewStore.mode）：

| Tab | 内容 |
|-----|------|
| 桌游创作 | 项目列表、搜索、创建/打开/导入（原「卡牌创作」/「项目」） |
| 对战 | RoomList + 邀请组件 |
| 市场 | MarketPage embedded |
| 论坛 | ForumPage embedded |
| 新闻 | NewsPage embedded |

文案：**卡牌创作 → 桌游创作**（含落地页特性卡片、onboarding「卡牌创作 → 案例」）。

**项目操作**：

- 新建：CreateProjectDialog — 选 starter、名称、可选本地文件夹
- 打开：列表点击、**选文件夹**、导入 project.json
- **拖入打开**：把项目文件夹、`.ceditor`、或 `project.json` 拖到首页即可打开。不是 TMD 工程（没有 `.ceditor` / `data/project.json` 结构、或 zip/散图等不支持的文件）要明确说明，不要静默失败
- 右键：编辑元数据、导出 json/zip、在资源管理器显示、重链文件夹、忘记、取消订阅、**复制为新工程**
- **复制为新工程**：若源工程已有本机文件夹，在目标路径 **拷贝整个文件夹** 再改项目名/id，禁止把内嵌贴图的整包 JSON 一次 POST（大会失败并留下空 `data/`）。中文路径与空格可用。目标若只有空 `data/` 残留，允许覆盖写入。
- 路径丢失检测（PATH_LOST、folderGone）
- 书架视图切换（homeViewStore.shelf）

**Starter 模板**（见 data-model）：empty, poker, sanguosha, mtg, pokemon, mahjong, uno, halligalli

### 工作区（/project）

`[反推]` WorkspaceLayout：

**侧栏导航**（产品已确认的维度）：

1. **卡牌** — 其下：**卡牌规格**（预览块）→ 蓝图（预览块）→ 数据集、卡组。见 [pieces.md](./pieces.md)
2. **板件** — 预览块 + 右侧改贴图/厚度，点块不跳页。见 [boards.md](./boards.md)
3. **包装盒** — 先包装库预览块，再进结构 / 贴图 / 渲染，见 [packaging.md](./packaging.md)
4. **说明书** — 见 [rulebook.md](./rulebook.md)

项目级（各维度共用）：变量、媒体、舞台、**产品渲染**（先场景预览块，再点进内部编辑，见 [product-render.md](./product-render.md)）、**影棚**（先影棚预览块，再选模版填演员，见 [studio.md](./studio.md)）、打印、设置

`[反推]` 当前侧栏仍是扁平：模板、卡牌集、卡组、变量、媒体、舞台、打印、设置。

**顶栏**：

- 保存、撤销/重做、导出 zip/json、快捷键帮助（?）
- 项目名称、Brand、PrefsMenu

**行为**：

- 未打开项目 → 重定向 `/app`
- dirty 后 500ms debounce 自动 persist
- Ctrl+S 保存；Ctrl+Z/Y 撤销重做
- 切换路由时 editorStore 会话保持
- OnboardingDock 新手任务

### 变量页（/project/vars）

- 编辑 ProjectVariable 全局替换
- tag / replacement / kind(text|icon)
- 进入变量页时，**仍显示卡牌二级栏**（卡牌规格 / 蓝图 / 卡牌集 / 卡组），点栏即可跳到对应页。变量本身仍是顶栏项目级入口，不要把人困在没有二级栏的空顶栏里。

### 媒体页（/project/media）

布局：**撑满工作区内容区**，不要居中窄栏（禁止 `max-width: 1100px` 一类把网格挤窄）。

顶栏：

- 筛选、上传
- **一键更新**：对库里每一个素材，按它已记录的工程路径（`assets/…` 或 `/__fs/file` 的磁盘路径）去磁盘再读该文件，**写回同一 `assetId`**，清缓存并刷新所有引用预览。不弹逐个选文件。找不到路径的（纯 data URL、文件已删）跳过，结束后提示成功/跳过数量。
- 「覆盖当前」仍保留：给**某一个** id 换一份新文件（可改路径）

筛选 Tab（与搜索叠用）：

| Tab | 内容 |
|-----|------|
| 全部 | 所有 `project.assets` |
| 未引用 | 蓝图图层、卡牌集字段、包装贴图、封面、字体、变量图标都没用到 |
| 已引用 | 至少被上面任一处用到 |

引用判定要扫：蓝图 image/icon 图层、卡牌集/Deck 字段、包装盒根上的 `textureAssetId` / 烫金 UV 遮罩，以及详细模式 `lid`/`base` 的外壁、内壁、烫金、UV 遮罩、板件贴图、封面、字体、变量。未扫到的会在素材库被标成「未引用」，容易误删。

同名再导入只认 **完整文件名**（路径最后一段，含扩展名）。禁止用后缀模糊匹配：`盒底_CMYK.jpg` 不得覆盖 `天地盒_盒底_CMYK.jpg`。中文文件名必须能从 `/__fs/file` 读出；磁盘上已有同一相对路径时，扫描不得再登记第二个 id（避免路径重复、素材库一对多）。

**点击素材** → 打开**次级面板**（与 UV 编辑器同一套「画布 + 右侧栏」心智）：

- 大图预览
- 详细信息：assetId、种类、像素尺寸（能读到时）、**磁盘/工程路径**（可复制）
- **重新链接**：选新文件覆盖该 id（同「覆盖当前」）
- 复制 id、删除

网格仍用图鉴 `Pager`。

- 同名再导入 = 覆盖，不是新建一份
- 必须刷新所有引用：蓝图图层、卡牌集/Deck 预览、包装盒贴图、封面、变量图标、对战牌面
- 清掉该 id 的图片缓存（Canvas / 缩略图 / 3D 贴图），禁止继续显示旧图

### 设置页（/project/settings）

- 项目 meta：名称、备注、封面、默认尺寸
- 字体管理

## 数据与状态

- AppIndex（IndexedDB）— 项目列表
- Project — 打开后载入 appStore.current
- 文件夹格式 — ceditor-folder-v1
- CreateProjectDialog → createProject / openFromDirectory

## 验收标准

- [ ] 可从 starter 创建项目并进入工作区
- [ ] 打开/保存/导出 zip 与 json 正常
- [ ] 可把项目文件夹拖到首页打开；拖入非工程文件要说明原因
- [ ] 选本地文件夹后 assets 相对路径可重开
- [ ] 路径丢失时提示重链
- [ ] 工作区侧栏导航各页可达
- [ ] 首页 Tab 显示「桌游创作」而非「卡牌创作」
- [ ] 工作区能切换卡牌、板件、包装盒、说明书四个维度；项目级有产品渲染、影棚入口
- [ ] 自动保存与 dirty 指示正确
- [ ] 素材可覆盖同一 id，引用处预览更新；图鉴分页可跳转
- [ ] 天地盒 lid/base 贴图算已引用；中文文件名可预览；同一磁盘路径只有一个 assetId
- [ ] 素材库撑满页面；Tab 全部/未引用/已引用；点开次级面板可见路径并可重新链接
- [ ] 「一键更新」按各素材已有路径从磁盘刷新，不改 assetId
- [ ] 变量页能看到并点进蓝图 / 卡牌集 / 卡组二级栏
- [ ] 关闭项目返回首页列表更新
- [ ] 落地页与首页「下载与发送」可分别下载 Windows / Mac 离线包（缺哪份禁用哪份）

## 已知限制 / 不做

`[反推]`

- 浏览器内无法直接打开系统资源管理器（仅 Tauri/Node 扩展）
- 无云同步账号体系
- 示例项目 seed 后标记 origin=example

## 变更记录

| 日期 | 说明 |
|------|------|
| 2026-09-02 | `[反推]` 初稿 |
| 2026-09-02 | 卡牌创作更名为桌游创作；工作区按卡牌/板件、包装盒、说明书分维 |
| 2026-09-02 | 素材覆盖同一链接；图鉴分页 1…n + 跳转 |
| 2026-09-02 | 一键按路径更新；素材 Tab 与次级预览；变量页保留蓝图/卡牌集栏 |
| 2026-09-02 | 项目级入口「产品渲染」 |
| 2026-09-02 | 产品渲染与蓝图/包装盒一样：先库预览块再进编辑 |
| 2026-09-08 | 卡牌/板件二级栏最前增加「卡牌规格」预览块 |
| 2026-09-16 | 板件从卡牌拆出，与包装盒并列成独立 Tab |
| 2026-09-16 | 打开文件夹用系统标准对话框（默认 TMD.exe 目录）；首页可拖入工程 |
| 2026-09-10 | 落地页 / 首页下载 Windows 与 Mac（Apple 芯片）两份离线 zip |
| 2026-09-18 | 项目级入口「影棚」 |
