---
description: 画面还不是最终结果时必须工作锁，挡住误操作
alwaysApply: true
---

# 体验未就绪时用工作锁

用户刚改完、预览却还在读印刷图 / 出图 / 写盘、画面还不是最终结果时，**不要让人继续点**。否则会当成加载失败。转镜头、改底色、只改网格开口这类立刻能画完的，不是工作锁。

## 必须

- 盖共用工作锁（虚化 + 挡住 pointer），标题写正在做什么
- 有步数显示 `n / m`，没有就用不确定条；超过约 1 秒补已用时
- 短于约 80ms 不要闪一下；结束立刻拿掉
- 规格见 `docs/features/ui-consistency.md`「工作锁」

## 不要锁

**只有质变还没完成、人必须等** 才出锁：第一次把印刷图从磁盘/缓存读进来、出图写 PNG、写盘、**影棚写序列帧**。

**不要锁、也不要为此重建模型或重载贴图**（转镜头也会进 undo 合并，但画面立刻就是最终结果）：

- 摄像机轨道旋转 / 平移 / 滚轮缩放
- 灯光、背景、分辨率红框、选中面
- 底色 / 金属度 / 反光度
- 开合滑条预览、盒子位姿
- 影棚播放条、开盒/转台/展开秒数
- 只改网格朝向或 UV 壳位置（同步改完，不读图）

套合轴向只改开口和网格，**不得**当成贴图重载去盖工作锁。禁止「任何 patchProject 都 loading」。

