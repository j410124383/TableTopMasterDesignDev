---
description: 所有更新文档先行 — docs/ 是唯一真相来源
alwaysApply: true
---

# 所有更新文档先行

任何功能、交互、数据结构、UI 变更，**先改 `docs/`，再改代码**。对话里的需求不是开工许可。

## 必须先做

1. 改对应 `docs/features/*.md`（界面与交互 + 验收标准）
2. 有新字段则先改 `docs/data-model.md`（禁止先改 `types.ts`）
3. 在 `docs/changelog.md` 追加 `[待同步] 范围 - 简述`
4. **停下**。等用户说「按文档同步」或「实现 changelog 里待同步的项」

## 禁止

- 跳过 spec，按聊天碎片改 UI / 交互 / 数据
- 文档未更新时扩展产品行为
- 文档与代码冲突时改文档去迁就代码（用户明确「先改代码」除外）
- 改用户写在 changelog 里的 `[待同步]` 意图（只能改状态）

## 例外

- **纯 bugfix**（文档已有行为，实现写错）：可先改代码，changelog 补 `[待补文档]`
- 实现成本高：先说明，等用户砍 scope 或分期，仍不先写代码

## 按文档同步时

读 changelog `[待同步]` → 对照 feature → 列出差异 → 最小 diff → 对照验收标准 → 改为 `[已同步 vX.Y.Z]`

入口：`docs/README.md`、`AGENTS.md`
